import { styled } from "../../../../stitches.config";

export const AppLayoutContainerStyle = styled("div", {
  display: "flex",
  height: "100%",
  minHeight: "100%",
});

export const AppLayoutContentStyle = styled("main", {
  width: "100%",
});