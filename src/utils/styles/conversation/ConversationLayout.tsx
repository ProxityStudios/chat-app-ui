import { styled } from "../../../../stitches.config";

export const ConversationLayoutContainerStyle = styled("div", {
  height: "100%",
  display: "flex",
});

export const ConversationLayoutContentStyle = styled("div", {
  width: "100%"
}); 